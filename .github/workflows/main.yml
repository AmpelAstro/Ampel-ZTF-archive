name: ci

on:
  push:
    branches:
      - main
      - renovate/*
      - dev/*
    tags:
      - v*
  pull_request:
    branches:
      - main
      - dev/*
      - stable/*

jobs:
  ci:
    uses: AmpelProject/Ampel-interface/.github/workflows/ci.yml@2a1c618613ed2b80480db71a557f57cb44c33b34
    secrets:
      PYPI_API_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
    with:
      mongo: false
      lint: false
      format: false
      # renovate: datasource=conda depName=conda-forge/python
      python-version: "3.9"
      # renovate: datasource=pypi depName=ruff
      ruff-version: "0.4.5"
